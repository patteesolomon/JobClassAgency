import React from 'react';
import EmpTypes from './EmpTypes';

export default function JobList()
const [todos]
{
  return (
    <EmpTypes Clientelle ={this.Client.name}>
        <form onSubmit={this}
    </EmpTypes>
  );
}
